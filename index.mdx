---
title: "Introduction"
description: "Enrich company and people data programmatically with the Origami API."
---

The Origami API lets you push raw company or contact data into your Origami tables and get
enriched results back — emails, employee counts, technographics, and everything else your
table's enrichment columns produce.

<Tip>
  Need the full API spec? [Download the OpenAPI specification](https://raw.githubusercontent.com/Origami-Agents/mintlify-docs/main/openapi-v1.yaml) for use with AI coding assistants, client generators, or Postman.
</Tip>

## How it works

<Steps>
  <Step title="Create an API key">
    Go to **Settings → API Keys** in the Origami app and generate a key.
    It starts with `og_live_` and is shown once — save it securely.
  </Step>
  <Step title="List your tables">
    Call `GET /tables` to see your tables, their columns, and their slugs.
    Find the table you want to enrich into.
  </Step>
  <Step title="Insert rows">
    `POST /tables/{tableId}/rows` with your raw data. Origami starts enriching
    immediately and returns a `batchId`.
  </Step>
  <Step title="Poll for results">
    Call `GET /batches/{batchId}` to check progress. When complete, the response
    includes all enriched row data and credits used.
  </Step>
</Steps>

## Quick links

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    End-to-end walkthrough with curl examples.
  </Card>
  <Card title="Authentication" icon="key" href="/authentication">
    How to create and manage API keys.
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference">
    Full endpoint documentation with interactive playground.
  </Card>
  <Card title="Credit balance" icon="coins" href="/api-reference/credits/get-credit-balance">
    Check your remaining credits before enriching.
  </Card>
</CardGroup>

## Base URL

All API requests go to:

```text
https://origami.chat/api/v1
```

## Async model

All write endpoints are **asynchronous**. A `POST` returns a `batchId` immediately —
poll `GET /batches/{batchId}` to check progress and retrieve enriched results.

- For fast results, poll every **2–5 seconds**.
- For background processing, poll every **30–60 seconds**.
